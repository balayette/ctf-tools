Easy instruction counting with QEMU, in both user and system mode.
Supports i386/x86_64 userspace QEMU, and all architectures of system QEMU.
Build the patched QEMU, symbolic link it to ./patched_qemu, and then read
./counting.py --help

gdbcount.py does instruction counting with gdb, patch it to suit your needs.
